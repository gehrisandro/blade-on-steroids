<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Prefix delimiter
    |--------------------------------------------------------------------------
    |
    | Specify the delimiter that will be used to separate the prefix from the
    | attribute name. Default is '::'.
    */

    'prefix_delimiter' => env('BLADE_PREFIX_DELIMITER', '::'),
];
